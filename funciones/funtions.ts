(( ) => {
    
    const hero:string = 'Peter Carvajal';

    function returnName():string{
        return hero;
    }

    const batiSenaActivada = ():string => 'Batisena Activada !'

        console.log (returnName());
        console.log (batiSenaActivada());

        const heroName = returnName();
})( )